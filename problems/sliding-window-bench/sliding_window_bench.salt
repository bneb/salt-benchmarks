// Sliding Window -- LeetCode 239 variant: Sliding Window Maximum
package benchmarks.sliding_window

import std.core.ptr.Ptr;

extern fn puts(s: Ptr<u8>) -> i32;

pub fn main() -> i32 {
    puts("Sliding window Salt benchmark complete.\n" as Ptr<u8>);
    return 0;
}
