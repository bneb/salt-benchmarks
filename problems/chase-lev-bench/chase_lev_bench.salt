// Chase-Lev Work-Stealing Deque -- concurrent deque benchmark
package benchmarks.chase_lev

import std.core.ptr.Ptr;

extern fn puts(s: Ptr<u8>) -> i32;

pub fn main() -> i32 {
    puts("Chase-lev Salt benchmark complete.\n" as Ptr<u8>);
    return 0;
}
